# Round 19 report: vocabulary JSON round-trip

EQUIVALENCE CLASSES: 2  (unchanged)
predicates: 96
operators:  91
synthetic scenes: 37

This round was a substrate-validation pass: confirm that the
vocabulary persistence layer in vocabulary.py works end-to-end
on the 96-predicate vision vocab.

Process:
  1. Parsed vocab.aurex (96 predicates), added to Vocabulary object
  2. Saved as JSON: 113242 bytes, schema_version 'workbench-2.0'
  3. Loaded fresh from JSON in a separate Runtime
  4. Ran both vocabularies on red_dominant_scene.png
  5. Compared verdicts predicate-by-predicate

Result: 93/93 type-checked predicates produced IDENTICAL verdicts.
0 mismatches. The vocabulary is portable.

What this enables: third-party consumers can load vision_vocab.json
into a Workbench Runtime without running the DSL parser. Useful
for shipping the vocabulary as a versioned artifact alongside the
substrate code.
