# Round 18 report: quality maintenance pass

EQUIVALENCE CLASSES: 2  (unchanged)
predicates: 96
operators:  91 (added orientation_distribution_continuity, top_vs_bottom_orientation_difference)
synthetic scenes: 37 (added curve_scene)

Round 18 was a maintenance pass:
1. scipy.ndimage.label installed; bright_spot_count and blob_count_thresh
   now use vectorised labeling (sub-millisecond on 256x256 inputs).
2. has_curved_signature redesigned with positive test:
   width-above-0.7-max + cyclic-contiguity allowing 1 stray bin.
   Now fires on curve_scene (0.57), zero on lines/grids/circles.
3. has_perspective_convergence redesigned with top-vs-bottom dominant
   orientation. The perspective_road synthetic still doesn't fire
   (lines symmetric around vanishing point average to similar
   orientations top vs bottom). Filed as a known-weak-but-correct
   detector pending a Hough-transform implementation.

Methodology rule: when retiring a "negation-of-everything-else"
predicate, replace it with a POSITIVE test. has_curved_signature went
from 0% firing across 12 rounds to discriminating correctly on the
synthetic that should fire it.
