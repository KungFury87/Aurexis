# Round 21 report: vocab_diff + vignette + identity architecture

EQUIVALENCE CLASSES: 2  (unchanged from round 15)
predicates: 99 (was 96; added 3 identity-LIKE composites)
operators:  91
synthetic scenes: 38 (added vignette_scene)

## Three changes landed

### A. vocab_diff.py
Structured diff between two vision_vocab.json snapshots. Reports
predicates added/removed, body changed, intent renamed,
expects/return-type changed. Demonstrated by diffing a synthetic
"pre-lighting" snapshot against round 20: correctly identifies
the 8 lighting predicates as added. Round 20 vs round 21 diff
shows 3 predicates added (the identity-LIKE composites).

### B. Vignette synthetic
vignette_scene synthetic added. has_edge_weighted_lighting now
fires on its target. Down to ZERO always-False predicates across
the 99-predicate vocabulary.

### C. Identity layer architecture (interface only, no ML)
IDENTITY_LAYER_DESIGN.md documents:
  - new dtype: identity_label
  - new operator family: external_classifier(image, key, label)
  - runtime hook: register_classifier(key, callable)
  - convention: BLOCKED if no classifier registered for key

Three identity-LIKE composite predicates added to vocab.aurex
(no ML, just patterns over existing perceptual operators):
  has_human_subject_signature
  has_indoor_scene_signature
  has_screen_subject_signature

These approximate identity categories from perceptual content.
Real identity recognition waits until a model plugs into the
external_classifier hook.

## Vocabulary cleanliness state (after round 21)

  predicates:                 99
  operators:                  91
  synthetic scenes:           38
  always-False predicates:    0  (was 5 at round 14; vignette retired the last one)
  empirical structural redundancies: 0
  tautological pairs:         1  (largely_achromatic / low_saturation / monochrome)
  corpus-size artifact pairs: 1  (drift / motion_upward)
  hardware-blocked predicates: 3 (raw Bayer, polarization, multispectral)

The perceptual side of the vocabulary is now empirically clean by
every measure the IR loop tracks.
